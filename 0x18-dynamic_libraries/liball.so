#!/bin/bash

# Compile all the .c files in the current directory as position-independent code and generate object files
gcc -Wall -Werror -Wextra -pedantic -std=gnu89 -c -fPIC *.c

# Create the dynamic library liball.so from the object files
gcc -shared -o liball.so *.o

# Cleanup the object files
rm *.o
